package desktop.pages;

import abstractclasses.*;

public class Home extends AbstractPage {

    public Home() {
        setPageUrl(Urls.BASE_URL);
        setPageUrlPattern(UrlPatterns.BASE_URL_PATTERN);
    }
}
